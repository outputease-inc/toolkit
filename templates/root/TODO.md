# TODO - Future Feature Ideas

High-level ideas for future development. These are captured as they
emerge and refined into actionable work when prioritized.

Ideas progress through stages: `[Raw]` -> `[Developing]` -> `[Ready: Direct]` or `[Ready: Spec]`.
- `[Ready: Direct]` — implement in the current branch, no formal spec needed.
- `[Ready: Spec]` — promote to a formal spec via `/speckit-specify`.

Use `/develop-idea [idea-name]` to collaboratively develop a `[Raw]` idea, then choose Direct or Spec at readiness.
Use `/quickstart` to scan this file and discover promotion candidates.
Use `/speckit-specify [idea-name]` to promote a `[Ready: Spec]` idea to a formal specification.

---

## Ideas Backlog

_No pending ideas_

<!-- Example entries (delete these when adding your first real idea):

### `[Raw]` Dark mode toggle
User requested dark mode support. No research done yet.

### `[Developing]` Email notification system
- Trigger: After user completes signup
- Open questions: Which email provider? Resend vs SendGrid?
- See `/develop-idea email-notifications` for brainstorm output

### `[Ready: Direct]` Fix dashboard pagination off-by-one
- Single-file fix in `src/components/Pagination.tsx`
- No spec needed; implement in current branch
- Auto-removal prompt fires on `/session-end`

### `[Ready: Spec]` CSV export for dashboard data
- Users need to export filtered table data as CSV
- Scope: Dashboard page only, client-side generation
- Ready to promote: `/speckit-specify csv-export`
-->

---

<!-- Date only (YYYY-MM-DD). Never append changelog prose; git history is the log. -->
**Last Updated**: [DATE]
