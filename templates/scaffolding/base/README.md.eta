# <%= `${it.projectName}\n\n` %>Scaffolded with [OutputEase Toolkit](https://github.com/outputease-inc/toolkit).

## Getting Started

```bash
<%= `${[it.pm.install, `${it.pm.run} dev`].join("\n")}\n` %>```

## Scripts

| Command | Description |
|---------|-------------|
| `<%= it.pm.run %> dev` | Start development server |
| `<%= it.pm.run %> build` | Build for production |
| `<%= it.pm.run %> lint` | Check code with Biome |
| `<%= it.pm.run %> format` | Format code with Biome |
| `<%= it.pm.run %> typecheck` | Run TypeScript type checking |

## Tech Stack

<%= `${it.stackSummary.map((tool) => `- ${tool}`).join("\n")}\n` %>
